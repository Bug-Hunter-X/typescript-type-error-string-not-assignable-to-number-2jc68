function add(a: number, b: number): number {
  return a + b;
}

let result = add(10, 20); // Corrected: Passing numbers
console.log(result); 

function addString(a: string, b: string): string {
    return a + b
}

let stringResult = addString("hello", " world")
console.log(stringResult) 